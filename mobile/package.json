{
  "name": "ferrovis-mobile",
  "version": "1.0.0",
  "main": "index.ts",
  "scripts": {
    "start": "expo start",
    "android": "expo start --android",
    "ios": "expo start --ios",
    "web": "expo start --web",
    "lint": "npx eslint . --ext .ts,.tsx || echo 'No ESLint config found, skipping lint'",
    "test": "jest || echo 'No tests configured yet'",
    "format": "prettier --write . || echo 'No Prettier config found, skipping format'",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "expo": "~53.0.9",
    "expo-status-bar": "~2.2.3",
    "react": "19.0.0",
    "react-native": "0.79.2"
  },
  "devDependencies": {
    "@babel/core": "^7.25.2",
    "@types/react": "~19.0.10",
    "typescript": "~5.8.3"
  },
  "private": true
}
